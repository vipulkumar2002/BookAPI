// MongoDB Operators -> esse interview me ques. puchhate hai in mongoDB
/* 
---------- Update operators ---------------- 

    1) $inc -> Increment
    2) $min -> minimum
    3) $max -> maximum
    4) $set -> sets a data to an object property
    5) $unset -> remove a property from an object

------------ Arrays operators -----------

    1) $push -> appends a specified value to an array
    2) $pop -> removes the first or last element of an array
    3) $pull = similer to filter ->  remove all the instances of a values from a MongoDB document
    4) $addToSet -> adds a value to an array unless the value is already present
    
*/ 